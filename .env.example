# Backend (optional — defaults shown)
# SECUREFLOW_THRESHOLD=35

# Optional: Groq API key for AI-generated explanations on flagged transactions.
# If set, Groq (llama-3.1-8b-instant) is used; otherwise template-based text is used.
# Get a key at https://console.groq.com
# GROQ_API_KEY=your_key_here

# Frontend: API base URL (empty = use Vite proxy to http://localhost:8000)
# VITE_API_BASE_URL=
